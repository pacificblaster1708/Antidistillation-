# Core requirements for top-K soft distillation.
#
# These are the versions this repo was developed and tested against. They match
# the pins used by the locuslab/antidistillation-sampling repo, so the two can
# share one environment. Newer versions generally work; see README "Version
# compatibility" before upgrading transformers past 4.5x.

torch>=2.4.0
transformers>=4.45.0,<5.0.0
tokenizers>=0.20.0
datasets>=3.0.0
accelerate>=1.2.0
peft>=0.14.0
numpy>=1.24.0

# Optional but strongly recommended on Ampere/Hopper GPUs.
# Install separately -- it needs an isolated build step:
#   pip install flash-attn==2.7.4.post1 --no-build-isolation
# The code falls back to PyTorch SDPA automatically if it is missing.
