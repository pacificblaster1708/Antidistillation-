# Verified working with the pinned versions in the right-hand comments.
torch>=2.3                 # tested with 2.6.0
transformers>=4.45,<5      # tested with 4.51.3
datasets>=3.0              # tested with 5.0.1
accelerate>=0.34           # tested with 1.14.0
peft>=0.13                 # tested with 0.20.0  (only needed when lora=true)
math-verify>=0.5           # tested with 0.9.0   (optional: data.py falls back to
                           #                      boxed-answer matching without it)

# Optional, GPU only. The code auto-falls back to sdpa when it is absent:
#   pip install flash-attn --no-build-isolation
